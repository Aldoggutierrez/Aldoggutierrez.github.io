# Token de GitHub — opcional, solo aumenta el rate limit (60 → 5000 req/hora)
# Genera uno en: https://github.com/settings/tokens (sin scopes necesarios para repos públicos)
# Copia este archivo como .env y pega tu token
GITHUB_TOKEN=
